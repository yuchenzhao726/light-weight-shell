#ifndef _COMMAND_HANDLER_H_
#define _COMMAND_HANDLER_H_

int command_handler(const char *const command);

// return 0 if the execution is successful and will continue. return 1 when exit. 
int cmd_handler(const char *const cmd);

#endif